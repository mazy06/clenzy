# ============================================================
# Clenzy PMS — Variables d'environnement requises
# ============================================================
# Copiez ce fichier en .env et remplissez les valeurs.
# NE JAMAIS committer le fichier .env dans le repository.
# ============================================================

# ─── Base de donnees PostgreSQL ──────────────────────────────
SPRING_DATASOURCE_USERNAME=clenzy
SPRING_DATASOURCE_PASSWORD=             # OBLIGATOIRE — mot de passe PostgreSQL

# ─── Keycloak (authentification OAuth2/OIDC) ─────────────────
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=                # OBLIGATOIRE — mot de passe admin Keycloak
KEYCLOAK_CLIENT_SECRET=                 # Secret du client clenzy-web

# ─── Chiffrement (Jasypt / AES-256-GCM) ─────────────────────
JASYPT_ENCRYPTOR_PASSWORD=              # OBLIGATOIRE — cle maitre de chiffrement des tokens
# clenzy.encryption.key-version=1       # Version de cle courante (defaut: 1)
# clenzy.encryption.previous-passwords= # Anciennes cles pour rotation (comma-separated)

# ─── Stripe (paiements) ─────────────────────────────────────
STRIPE_SECRET_KEY=                      # OBLIGATOIRE — cle secrete Stripe
STRIPE_PUBLISHABLE_KEY=                 # OBLIGATOIRE — cle publique Stripe
STRIPE_WEBHOOK_SECRET=                  # Secret du webhook Stripe

# ─── Redis ────────────────────────────────────────────────────
# SPRING_REDIS_PASSWORD=                # Mot de passe Redis (prod uniquement)

# ─── Kafka ────────────────────────────────────────────────────
# SPRING_KAFKA_BOOTSTRAP_SERVERS=clenzy-kafka:9092
# KAFKA_SECURITY_PROTOCOL=PLAINTEXT     # SASL_SSL en production
# KAFKA_SASL_MECHANISM=SCRAM-SHA-512
# KAFKA_SASL_JAAS_CONFIG=               # Config JAAS Kafka

# ─── Airbnb Partner API ──────────────────────────────────────
AIRBNB_CLIENT_ID=
AIRBNB_CLIENT_SECRET=
AIRBNB_REDIRECT_URI=http://localhost:8084/api/airbnb/callback
AIRBNB_WEBHOOK_SECRET=                  # OBLIGATOIRE si webhooks actives

# ─── Minut (monitoring sonore) ───────────────────────────────
MINUT_CLIENT_ID=
MINUT_CLIENT_SECRET=
MINUT_REDIRECT_URI=http://localhost:8084/api/minut/callback
MINUT_WEBHOOK_SECRET=

# ─── Tuya Cloud API (capteurs IoT) ──────────────────────────
TUYA_ACCESS_ID=
TUYA_ACCESS_SECRET=
# TUYA_API_BASE_URL=https://openapi.tuyaeu.com
# TUYA_REGION=eu

# ─── Email (SMTP) ────────────────────────────────────────────
MAIL_HOST=clenzy-mailpit
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
# MAIL_SMTP_AUTH=false
# MAIL_SMTP_STARTTLS_ENABLE=false
# MAIL_FROM=info@clenzy.fr

# ─── Frontend ────────────────────────────────────────────────
FRONTEND_URL=http://localhost:3000
