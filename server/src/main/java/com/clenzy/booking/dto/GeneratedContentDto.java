package com.clenzy.booking.dto;

/**
 * Contenu généré par IA pour le Booking Engine (CLZ Domaine 2 — outils IA) :
 * description de bien ou meta SEO, dans la langue demandée (fr/en/ar).
 *
 * @param kind     "DESCRIPTION" ou "SEO_META"
 * @param language langue du contenu (fr/en/ar)
 * @param title    titre SEO (null pour une description)
 * @param content  corps généré (description, ou meta description pour SEO_META)
 */
public record GeneratedContentDto(
        String kind,
        String language,
        String title,
        String content
) {}
