package com.clenzy.model;

/** Origine d'un contact marketing capturé (CLZ Domaine 2 — capture de leads). */
public enum MarketingContactSource {
    NEWSLETTER,
    WAITLIST,
    EXIT_INTENT,
    ABANDONED_CART,
    OTHER
}
