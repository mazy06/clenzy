package com.clenzy.model;

/** Catégories de services additionnels vendus au guest depuis le livret. */
public enum UpsellType {
    EARLY_CHECKIN,
    LATE_CHECKOUT,
    CLEANING,
    TRANSFER,
    BREAKFAST,
    PARKING,
    EQUIPMENT,
    EXPERIENCE,
    OTHER
}
