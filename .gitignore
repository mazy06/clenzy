# Dependencies
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Production builds
build/
dist/

# SDK widget buildé (artefact `npm run build:sdk` / prebuild) — généré au build, non commité
client/public/booking/v1/
target/
**/target/

# TypeScript build info
*.tsbuildinfo

# Environment variables (secrets - ne jamais commiter)
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code worktrees (locaux, gérés par Claude Code, ne pas commiter)
.claude/worktrees/

# OS
.DS_Store
Thumbs.db

# Docker (ancien dossier infrastructure migre vers clenzy-infra)
infrastructure/

# Logs
*.log
logs/

# Firebase (credentials - ne jamais commiter)
firebase-service-account.json
**/firebase-service-account.json

# Temporary files
*.tmp
*.temp
booking-templates/dist/

# ─── Documentation interne : JAMAIS versionnee (audits, plans, strategie) ───
# Les documents restent sur disque mais hors git. Le corpus RAG embarque
# (server/src/main/resources/kb/) et README.md restent versionnes.
/docs/
/server/docs/
/analyse-concurrentielle/
/campagne/
/*.md
!/README.md
*.pdf
__pycache__/
