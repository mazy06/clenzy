/**
 * Ré-export de la primitive Baitly UI — `components/baitly/PeriodSegmented`.
 *
 * L'implémentation « Signature » (tokens MUI) a été retirée au profit du
 * remaster construit sur `ToggleGroup`, adopté par tous les écrans. Ce fichier
 * ne subsiste que pour les imports historiques `components/PeriodSegmented`.
 */
export { default, type SegmentedOption, type PeriodSegmentedProps } from './baitly/PeriodSegmented';
