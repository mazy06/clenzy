/**
 * Ré-export de la primitive Baitly UI — `components/baitly/FilterChipRow`.
 *
 * L'implémentation « Signature » (tokens MUI) a été retirée au profit du
 * remaster, adopté par tous les écrans. Ce fichier ne subsiste que pour les
 * imports historiques `components/FilterChipRow`.
 */
export { default, type FilterChipOption, type FilterChipRowProps } from './baitly/FilterChipRow';
