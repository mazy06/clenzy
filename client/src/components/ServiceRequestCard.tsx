/**
 * Ré-export de la primitive Baitly UI — `components/baitly/ServiceRequestCard`.
 *
 * L'implémentation « Signature » (tokens MUI) a été retirée au profit du
 * remaster, seul consommé (ServiceRequestsGridView). Ce fichier ne subsiste que
 * pour les imports historiques `components/ServiceRequestCard`.
 */
export {
  default,
  type ServiceRequestCardData,
  type ServiceRequestCardProps,
} from './baitly/ServiceRequestCard';
